<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/skop_pams.php");
	include_once("{$currDir}/skop_pams_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('skop_pams');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'skop_pams';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`skop_pams`.`id`" => "id",
		"`skop_pams`.`skop_pams`" => "skop_pams",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`skop_pams`.`id`',
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`skop_pams`.`id`" => "id",
		"`skop_pams`.`skop_pams`" => "skop_pams",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`skop_pams`.`id`" => "ID",
		"`skop_pams`.`skop_pams`" => "Skop pams",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`skop_pams`.`id`" => "id",
		"`skop_pams`.`skop_pams`" => "skop_pams",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`skop_pams` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'skop_pams_view.php';
	$x->RedirectAfterInsert = 'skop_pams_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Skop pams';
	$x->TableIcon = 'table.gif';
	$x->PrimaryKey = '`skop_pams`.`id`';

	$x->ColWidth = [150, ];
	$x->ColCaption = ['Skop pams', ];
	$x->ColFieldName = ['skop_pams', ];
	$x->ColNumber  = [2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/skop_pams_templateTV.html';
	$x->SelectedTemplate = 'templates/skop_pams_templateTVS.html';
	$x->TemplateDV = 'templates/skop_pams_templateDV.html';
	$x->TemplateDVP = 'templates/skop_pams_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: skop_pams_init
	$render = true;
	if(function_exists('skop_pams_init')) {
		$args = [];
		$render = skop_pams_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: skop_pams_header
	$headerCode = '';
	if(function_exists('skop_pams_header')) {
		$args = [];
		$headerCode = skop_pams_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: skop_pams_footer
	$footerCode = '';
	if(function_exists('skop_pams_footer')) {
		$args = [];
		$footerCode = skop_pams_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
