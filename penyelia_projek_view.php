<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/penyelia_projek.php");
	include_once("{$currDir}/penyelia_projek_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('penyelia_projek');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'penyelia_projek';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`penyelia_projek`.`id`" => "id",
		"`penyelia_projek`.`nama_penyelia`" => "nama_penyelia",
		"IF(    CHAR_LENGTH(`negeri1`.`nama_negeri`), CONCAT_WS('',   `negeri1`.`nama_negeri`), '') /* Negeri */" => "negeri",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`penyelia_projek`.`id`',
		2 => 2,
		3 => '`negeri1`.`nama_negeri`',
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`penyelia_projek`.`id`" => "id",
		"`penyelia_projek`.`nama_penyelia`" => "nama_penyelia",
		"IF(    CHAR_LENGTH(`negeri1`.`nama_negeri`), CONCAT_WS('',   `negeri1`.`nama_negeri`), '') /* Negeri */" => "negeri",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`penyelia_projek`.`id`" => "ID",
		"`penyelia_projek`.`nama_penyelia`" => "Nama penyelia",
		"IF(    CHAR_LENGTH(`negeri1`.`nama_negeri`), CONCAT_WS('',   `negeri1`.`nama_negeri`), '') /* Negeri */" => "Negeri",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`penyelia_projek`.`id`" => "id",
		"`penyelia_projek`.`nama_penyelia`" => "nama_penyelia",
		"IF(    CHAR_LENGTH(`negeri1`.`nama_negeri`), CONCAT_WS('',   `negeri1`.`nama_negeri`), '') /* Negeri */" => "negeri",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['negeri' => 'Negeri', ];

	$x->QueryFrom = "`penyelia_projek` LEFT JOIN `negeri` as negeri1 ON `negeri1`.`id`=`penyelia_projek`.`negeri` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'penyelia_projek_view.php';
	$x->RedirectAfterInsert = 'penyelia_projek_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Penyelia projek';
	$x->TableIcon = 'table.gif';
	$x->PrimaryKey = '`penyelia_projek`.`id`';

	$x->ColWidth = [150, 150, ];
	$x->ColCaption = ['Nama penyelia', 'Negeri', ];
	$x->ColFieldName = ['nama_penyelia', 'negeri', ];
	$x->ColNumber  = [2, 3, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/penyelia_projek_templateTV.html';
	$x->SelectedTemplate = 'templates/penyelia_projek_templateTVS.html';
	$x->TemplateDV = 'templates/penyelia_projek_templateDV.html';
	$x->TemplateDVP = 'templates/penyelia_projek_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: penyelia_projek_init
	$render = true;
	if(function_exists('penyelia_projek_init')) {
		$args = [];
		$render = penyelia_projek_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: penyelia_projek_header
	$headerCode = '';
	if(function_exists('penyelia_projek_header')) {
		$args = [];
		$headerCode = penyelia_projek_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: penyelia_projek_footer
	$footerCode = '';
	if(function_exists('penyelia_projek_footer')) {
		$args = [];
		$footerCode = penyelia_projek_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
