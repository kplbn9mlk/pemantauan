<?php
// This script and data application were generated by AppGini 5.97
// Download AppGini for free from https://bigprof.com/appgini/download/

	$currDir = dirname(__FILE__);
	include_once("{$currDir}/lib.php");
	@include_once("{$currDir}/hooks/kelulusan.php");
	include_once("{$currDir}/kelulusan_dml.php");

	// mm: can the current member access this page?
	$perm = getTablePermissions('kelulusan');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'kelulusan';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`kelulusan`.`id`" => "id",
		"`kelulusan`.`kelulusan_jawatankuasa`" => "kelulusan_jawatankuasa",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`kelulusan`.`id`',
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`kelulusan`.`id`" => "id",
		"`kelulusan`.`kelulusan_jawatankuasa`" => "kelulusan_jawatankuasa",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`kelulusan`.`id`" => "ID",
		"`kelulusan`.`kelulusan_jawatankuasa`" => "Kelulusan jawatankuasa",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`kelulusan`.`id`" => "id",
		"`kelulusan`.`kelulusan_jawatankuasa`" => "kelulusan_jawatankuasa",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`kelulusan` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'kelulusan_view.php';
	$x->RedirectAfterInsert = 'kelulusan_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Kelulusan';
	$x->TableIcon = 'table.gif';
	$x->PrimaryKey = '`kelulusan`.`id`';

	$x->ColWidth = [150, ];
	$x->ColCaption = ['Kelulusan jawatankuasa', ];
	$x->ColFieldName = ['kelulusan_jawatankuasa', ];
	$x->ColNumber  = [2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/kelulusan_templateTV.html';
	$x->SelectedTemplate = 'templates/kelulusan_templateTVS.html';
	$x->TemplateDV = 'templates/kelulusan_templateDV.html';
	$x->TemplateDVP = 'templates/kelulusan_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: kelulusan_init
	$render = true;
	if(function_exists('kelulusan_init')) {
		$args = [];
		$render = kelulusan_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: kelulusan_header
	$headerCode = '';
	if(function_exists('kelulusan_header')) {
		$args = [];
		$headerCode = kelulusan_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once("{$currDir}/header.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/header.php");
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: kelulusan_footer
	$footerCode = '';
	if(function_exists('kelulusan_footer')) {
		$args = [];
		$footerCode = kelulusan_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once("{$currDir}/footer.php"); 
	} else {
		ob_start();
		include_once("{$currDir}/footer.php");
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
